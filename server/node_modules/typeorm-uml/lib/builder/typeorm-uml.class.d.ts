import { Connection } from 'typeorm';
import { Flags } from '../types';
export declare class TypeormUml {
    build(configNameOrConnection: string | Connection, flags: Flags): Promise<string>;
    private getConnection;
    private getUrl;
    private download;
    private getPath;
    private getStyles;
}
